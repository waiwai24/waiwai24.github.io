const config = {
    githubUser: "waiwai24",  // 你的 GitHub 用户名
    repo: "0101",            // 存放文章的仓库名
    path: "",                // 存放文章的目录路径
    isDarkMode: localStorage.getItem('darkMode') === 'enabled' // 明暗模式，默认从 localStorage 读取
};